<div>
  <%= render :partial => "posters/button_to_new" %>

  <% @posters.each do |poster| %>
      <%= render :template => 'posters/show', :locals => {:poster => @poster = poster} %>
      <% if @posters.last != poster %>
          <hr/>
      <% end %>
  <% end %>

  <%= render :partial => "posters/button_to_new" %>
</div>
