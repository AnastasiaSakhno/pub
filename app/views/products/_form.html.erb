<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :incoming_price %><br />
    <%= f.text_field :incoming_price %>
  </div>
  <div class="field">
    <%= f.label :sale_price %><br />
    <%= f.text_field :sale_price %>
  </div>
  <div class="field">
    <%= f.label :total_count %><br />
    <%= f.text_field :total_count %>
  </div>
  <div class="field">
    <%= f.label :amount_per_one %><br />
    <%= f.text_field :amount_per_one %>
  </div>
  <div class="field">
    <%= f.label :measure %><br />
    <%= f.select :measure, options_for_select(Constants::Measure.all, @product.measure) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
