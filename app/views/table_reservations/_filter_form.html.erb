<%= form_tag(table_reservations_path, method: :get) do %>
  <%= select_tag :status, options_from_collection_for_select(statuses, :id, :name, @status) %>
  <%= date_select :search, :date, { start_year: Date.current.year, default: @date } %>
  <%= submit_tag "Filter" %>
<% end %>