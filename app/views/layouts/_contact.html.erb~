<% cache ('contact_partial') do %>
    <div class="hero-unit">
      <div class="row">
        <div>
          <address>
            <strong><%= t :address %></strong><br>
            <%= t :address_value %>
          </address>

          <address>
            <strong><%= t :work_time %></strong><br>
            <%= t :work_time_value %>
          </address>

          <address>
            <strong><%= t :phone %></strong><br>
            <% 3.times do |i| %>
              <abbr title="<%= t :phone_type_m_title %>"><%= t :phone_type_m %>:</abbr> <%= t "cellphone_value_#{i}" %>
              <br/>
            <% end %>
          </address>

          <address>
            <strong><%= t :email %></strong><br>
            <a href="mailto:#"><%= t :email_value %></a>
          </address>
        </div>

        <div>
          <%=
              # first variant
              # render :template => 'locations/index', :locals => {:locations => @locations = Location.all.to_gmaps4rails}
              # second variant (static picture but i get lat and lng from the gmaps4rails-geolocation)
              #location = JSON.parse(Location.find(2).to_gmaps4rails)[0]
              #image_tag "http://maps.google.com/maps/api/staticmap?size=600x400&sensor=false&zoom=16&markers=#{location['lat'].to_s}%2C#{location['lng']}"
              # third variant (static lat and lng)
              image_tag "http://maps.google.com/maps/api/staticmap?size=600x400&sensor=false&zoom=16&markers=48.4638%2C35.052"
          %>
        </div>
      </div>
    </div>
<% end %>
